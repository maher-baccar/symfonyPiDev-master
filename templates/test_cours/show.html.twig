{% extends 'base.html.twig' %}

{% block title %}TestCours{% endblock %}

{% block body %}
    <h1>TestCours</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ test_cour.id }}</td>
            </tr>
            <tr>
                <th>Contenu_test</th>
                <td>{{ test_cour.contenuTest }}</td>
            </tr>
            <tr>
                <th>Test_name</th>
                <td>{{ test_cour.testName }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ test_cour.note }}</td>
            </tr>
        </tbody>
    </table>
<div class="d-flex w-100 justify-content-end px-5">
    <a class="btn btn-primary" href="{{ path('app_test_cours_index')  }}">back to list</a>

    <a class="btn btn-success" href="{{ path('app_test_cours_edit', {'id': test_cour.id})}}">edit</a>

    {{ include('test_cours/_delete_form.html.twig') }}
    </div>
{% endblock %}
