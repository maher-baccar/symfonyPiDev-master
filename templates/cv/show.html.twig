
{% block title %}Cv{% endblock %}

{% block body %}
    <h1>Cv</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cv.id }}</td>
            </tr>
            <tr>
                <th>Compétances</th>
                <td>{{ cv.competances }}</td>
            </tr>
            <tr>
                <th>Experience</th>
                <td>{{ cv.experience }}</td>
            </tr>
            <tr>
                <th>Education</th>
                <td>{{ cv.education }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cv_index') }}">back to list</a>

    <a href="{{ path('app_cv_edit', {'id': cv.id}) }}">edit</a>

    {{ include('cv/_delete_form.html.twig') }}
{% endblock %}
